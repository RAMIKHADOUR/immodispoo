{% extends 'base.html.twig' %}

{% block title %}TypesAnnonce{% endblock %}

{% block body %}
    <h1>TypesAnnonce</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ types_annonce.id }}</td>
            </tr>
            <tr>
                <th>Type_annonces</th>
                <td>{{ types_annonce.typeAnnonces }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_types_annonce_index') }}">back to list</a>

    <a href="{{ path('app_types_annonce_edit', {'id': types_annonce.id}) }}">edit</a>

    {{ include('types_annonce/_delete_form.html.twig') }}
{% endblock %}
