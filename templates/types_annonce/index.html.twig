{% extends 'base.html.twig' %}

{% block title %}TypesAnnonce index{% endblock %}

{% block body %}
    <h1>TypesAnnonce index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Type_annonces</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for types_annonce in types_annonces %}
            <tr>
                <td>{{ types_annonce.id }}</td>
                <td>{{ types_annonce.typeAnnonces }}</td>
                <td>
                    <a href="{{ path('app_types_annonce_show', {'id': types_annonce.id}) }}">show</a>
                    <a href="{{ path('app_types_annonce_edit', {'id': types_annonce.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_types_annonce_new') }}">Create new</a>
{% endblock %}
