{% extends 'base.html.twig' %}

{% block title %}Categorys{% endblock %}

{% block body %}
    <h1>Categorys</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Categorie_bien</th>
                <td>{{ category.categorieBien }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categorys_index') }}">back to list</a>

    <a href="{{ path('app_categorys_edit', {'id': category.id}) }}">edit</a>

    {{ include('categorys/_delete_form.html.twig') }}
{% endblock %}
